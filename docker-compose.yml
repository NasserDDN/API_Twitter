version: '3.9'
services:
    backend:
        build: ./backend
        ports: 
            - 5000:5000

    redis:
        image: "redis"
        ports: 
            - 6379:6379

    frontend:
        build: ./frontend
        ports:
            - 8080:80
